const prompt = require('prompt-sync')();
let jogadas
let jogadores = []
var nun = +prompt("numero de jogadores ")
for(let i = 0; i<nun ; i++ ){
    for (var i=0; i<jogadores; i++){
        var jogador={};
        var jogadas=[];     
        jogador.nome = prompt (`Digite o nome do ${i+1}º jogador: `);

}